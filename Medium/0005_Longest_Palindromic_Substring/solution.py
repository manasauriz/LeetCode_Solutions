class Solution:
    def longestPalindrome(self, s: str) -> str:
        left, right = 0, 2
        current_left, current_right = 1, 1
        current = s[current_left, current_left]
        
        # check if there is a palindrome in current substring
        
        return
    
if __name__ == "__main__":
    factory = Solution()
    print(factory.longestPalindrome("papad"))